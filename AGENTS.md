# AGENTS.md — pocketkeyer-site

PocketKeyer の公開ウェブサイト。GitHub Pages で `https://jun1972.com` として公開される。

このリポジトリで作業するエージェント向けの規約。

---

## 役割分担

- **Jun**: 意思決定、内容の確認、git の push（デプロイ）
- **サイト制作エージェント**: HTML / CSS の作成、文言のドラフト、画像素材の生成
- **設計 Claude**: 事実関係のレビュー（別リポジトリでアプリ設計を担当）

**エージェントは commit / push を行わない。** ファイルを作成・編集し、Jun に報告する。
Jun が内容を確認してから push する。push が即デプロイになるため。

---

## このリポジトリは公開されている

**Public リポジトリである。** GitHub Pages を無料で使うための条件。

つまり:

- **ここに書いたものはすべて公開される。** 下書きも、コメントも、削除したつもりの履歴も
- 未発表の情報、社内向けの検討メモ、個人情報を置かない
- アプリのソースコードはここにない（別リポジトリ）。持ち込まない

---

## 技術的な制約

- **静的な HTML / CSS のみ。** ビルドステップを持たない
- JavaScript は最小限。無くて済むなら使わない
- フレームワーク（React / Vue / Next.js 等）を使わない
- WordPress などの CMS を使わない
- npm / package.json を追加しない
- `main` ブランチのルートがそのまま公開される

理由: サイトは 2〜4 ページの静的なもので、ビルドの複雑さに見合う規模ではない。
Jun がプログラマではないため、壊れたときに原因を追える単純さを優先する。

### 触ってはいけないファイル

- **`CNAME`**: カスタムドメイン設定。削除・変更するとサイトが落ちる
- `.nojekyll`（存在する場合）

### 入れてはいけないもの

- **アナリティクス、トラッキングスクリプト、広告タグ**
  - PocketKeyer アプリは「データを一切収集しない」ことを掲げている。
    サイトにトラッカーがあると、その主張と混同されて信頼を損なう
  - Cookie 同意バナーが必要になり、静的サイトの単純さが失われる
- 外部 CDN からのフォント・スクリプト読み込み（上と同じ理由。可能な限り自己完結させる）

---

## サイトの構成

```
/                           トップ
/pocketkeyer/               製品ページ
/pocketkeyer/support/       サポート（App Store 提出に必須）
/pocketkeyer/privacy/       プライバシーポリシー（App Store 提出に必須）
```

**サポートとプライバシーの 2 ページが最優先。** この 2 つの URL が実在して開けないと
App Store へ提出できない。トップと製品ページは後回しでよい。

将来 `/pockettrack/` などが並ぶ可能性があるが、**現時点では作らない。**

---

## 内容についての規約

### 事実の出典

サイトに書く PocketKeyer の機能・価格・対応環境は、
**プロダクトブリーフに書かれていることだけ**を使う。

ブリーフに無い機能・数値・表現を使いたくなったら、**書く前に Jun に確認する。**
推測で埋めない。

### 特に注意すること

- **4K 対応と書かない**（V1 は 1080p のみ）
- **未発表の上位製品（Pro / PocketTrack / Spatial）に一切触れない**
- **「プロ品質」「放送品質」「AI が自動で」などの誇張・不正確な表現を使わない**
- 有料版は「機能が増えるもの」ではなく「ロゴを外すもの」

詳細はプロダクトブリーフのセクション 7 を参照。

---

## 確認方法

ローカルで確認するときは、HTML ファイルをブラウザで直接開けばよい。
サーバーを立てる必要はない。

公開後の反映には数分かかることがある。
